// import React from 'react';
// import BoardList from './Board';

// const Mode1Board = ({ boards, onBoardClick }) => {
//     return (
//         <div>
//             {boards.map((board) => (
//                 <div key={board.id} onClick={() => onBoardClick(board.id)}>
//                     <BoardList board={board} />
//                 </div>
//             ))}
//         </div>
//     );
// };

// export default Mode1Board;